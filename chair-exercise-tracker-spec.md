# 의자운동 다이어리 — 앱 기획 스펙 문서

> 작성일: 2026-07-06
> 목적: 부모님(아버지/어머니)의 의자운동 8가지 기록 및 자녀의 대시보드 열람을 위한 웹앱
> 원본 콘텐츠 출처: 노션 페이지 "의자운동" (유튜브 영상 기반 정리)

---

## 1. 프로젝트 개요

50대 이후 하체 근력·낙상 예방을 위한 의자운동 8가지를 부모님이 매일 기록하고,
자녀가 대시보드로 꾸준함을 확인하며 응원할 수 있도록 돕는 웹앱.
핵심 동기는 **"성취감"** — 체크, 연속일수, 완주 연출, 통계를 통해 꾸준한 운동 습관을 유도한다.

기존 프로젝트(`MyAssetDashBD`, `Pension-tracer`)와 동일한 개발/배포 방식을 따르되,
Firebase 프로젝트는 완전히 분리한다.

---

## 2. 운동 데이터 (고정 콘텐츠)

노션 페이지 원본 기준, 8개 운동은 다음과 같다. 앱 내 상수(seed data)로 등록한다.

| ID | 이름 | 목적 | 목표 |
|---|---|---|---|
| 1 | 시트 투 스탠드 (Sit to Stand) | 대퇴사두근·둔근·코어 강화, 일어서는 힘 | 10회 |
| 2 | 시티드 카프 레이즈 (Seated Calf Raise) | 종아리 근육 강화, 혈액순환·붓기 완화 | 15회 × 3세트 |
| 3 | 체어 푸시업 (Chair Push-Up) | 가슴·어깨·팔 뒤쪽 강화, 버티는 힘 | 10회 |
| 4 | 스카플러 리트랙션 (Scapular Retraction) | 굽은 등·말린 어깨 교정, 척추 정렬 | 15회 |
| 5 | 시티드 레그 익스텐션 (Seated Leg Extension) | 무릎 관절 보호, 허벅지 앞 강화 | 좌우 각 10회 |
| 6 | 시티드 마치 (Seated March) | 균형 감각, 낙상 예방 | 좌우 번갈아 총 20회 |
| 7 | 체어 힙 힌지 (Chair Hip Hinge) | 엉덩이·햄스트링·코어, 걸음 안정화 | 10회 |
| 8 | 시티드 토라시크 로테이션 (Seated Thoracic Rotation) | 흉추 유연성, 목·어깨 긴장 완화 | 좌우 각 5회 |

---

## 3. 사용자 / 접근 구조

두 종류의 화면이 하나의 데이터를 공유하되, 접근 방식이 다르다.

| 화면 | 대상 | 접근 방식 |
|---|---|---|
| **오늘의 운동** (기록 화면) | 아버지, 어머니 | 로그인 없음. 기기별 고정 프로필 자동 진입 (최초 1회 "이 기기는 누구 것인가요?" 선택 → localStorage 저장). 언제든 프로필 변경 옵션 제공 |
| **기록 보기** (대시보드) | 아버지, 어머니, 자녀 | 구글 로그인(Firebase Auth) 필요. 부모님은 본인 기록만, 자녀는 아버지/어머니/둘 다 조회 가능 |

- 부모님용 기기(안드로이드 스마트폰) 기준으로 설계. 아이폰 관련 PWA 제약은 고려 대상 아님.
- 계정과 "프로필(father/mother)"은 매핑되어야 함 — 로그인한 구글 계정이 어떤 프로필의 데이터를 볼 권한이 있는지 서버(Firestore 보안 규칙)에서 검증 필요.

---

## 4. 핵심 기능

### 4.1 오늘의 운동 (기록 화면)
- 오늘 날짜, 8개 운동 카드 (이름/목적/목표 표시 + 체크 버튼)
- 운동별 메모: 체크 시점과 무관하게 **언제든 입력/수정 가능** (예: 나중에 "무릎이 좀 아팠음" 추가)
- 오늘 진행률 (n/8) 표시 — 링(도넛) 형태
- 연속 완주일수(스트릭) 배지
- 8/8 완료 시 축하 연출 (예: "완료 도장" 스탬프 애니메이션)
- 과거 날짜로 돌아가 기록을 남기거나 메모를 추가하는 기능 (당일 깜빡했을 경우 대비)

### 4.2 기록 보기 (대시보드)
- 프로필 전환: 아버지 / 어머니 / (자녀 계정에 한해) 둘 다 비교
- 기간 탭: 일 / 주 / 월 / 분기 / 년
  - **일**: 선택한 날짜의 8개 운동 체크 상태 + 메모 리스트
  - **주**: 최근 7일 완료 개수 막대그래프
  - **월**: 달력 히트맵 (완주일 진하게, 일부 완료 흐리게, 무기록 표시)
  - **분기**: 월별 평균 완료 개수 막대그래프 (3개월)
  - **년**: 월별 평균 완료 개수 막대그래프 (12개월)
- 통계 카드: 현재 연속일수, 최장 연속일수, 총 참여일수(1개 이상 완료한 날), 총 완주일수(8/8), 선택 기간 평균 완료율
- 메모 모아보기: 최근 메모 목록 (건강 이상 신호 파악용)

### 4.3 알림
- Firebase Cloud Messaging(FCM) 활용, 매일 저녁 8시 리마인드 푸시 알림 (안드로이드 기준, 별도 PWA 설치 불필요)
- 알림 시간은 추후 설정 화면에서 변경 가능하도록 확장 여지를 둔다 (v1에서는 고정 8시도 무방)

---

## 5. 데이터 모델 (Firestore)

```
profiles (collection)
  └ {profileId}                 // "father" | "mother"
       ├ name: string
       ├ ownerUid: string       // 이 프로필의 기록 화면을 소유한 구글 계정 uid (선택)
       └ createdAt: timestamp

records (collection)
  └ {profileId} (doc)
       └ days (subcollection)
            └ {date}            // "YYYY-MM-DD"
                 ├ exercises: {
                 │     "1": { done: boolean, memo?: string, memoUpdatedAt?: timestamp },
                 │     "2": { done: boolean, memo?: string },
                 │     ...
                 │     "8": { done: boolean, memo?: string }
                 │   }
                 ├ completedCount: number   // 0~8, done 개수 캐시
                 └ updatedAt: timestamp

users (collection)                // 구글 로그인 사용자 ↔ 접근 권한 매핑
  └ {uid}
       ├ role: "parent" | "child"
       └ allowedProfiles: string[]   // 예: father만 접근 가능, 또는 [father, mother]
```

**설계 근거**
- `completedCount`를 캐시해두는 이유: 대시보드에서 월/분기/년 단위로 조회할 때 매번 8개 항목을 순회하지 않도록 (읽기 비용 절감)
- `records/{profileId}/days/{date}` 구조는 특정 기간 조회 시 날짜 범위 쿼리가 쉬움
- `users` 컬렉션으로 권한을 분리해두면, 나중에 형제자매가 추가되어도 `allowedProfiles`만 조정하면 됨

---

## 6. Firestore 보안 규칙 방향 (요약)

- `records/*`: 읽기는 `request.auth.uid`가 `users/{uid}.allowedProfiles`에 해당 profileId를 포함할 때만 허용
- `records/*`: 쓰기(기록 화면)는 기기 고정 프로필 방식이므로 별도 익명 인증(Firebase Anonymous Auth) 또는 기기 토큰 방식 검토 필요 — **결정 필요 항목** (7장 참고)
- `profiles/*`, `users/*`: 본인 문서만 읽기 가능, 쓰기는 관리자(자녀)만

---

## 7. 결정이 필요한 나머지 항목 (Claude Code 작업 전 확인)

1. **기록 화면의 쓰기 인증 방식**: 로그인 없이 기기에서 바로 쓰기가 가능해야 하므로, Firebase Anonymous Auth로 기기별 익명 계정을 부여하고 `profileId`와 매핑하는 방식을 제안. 확정 필요.
2. **자녀 계정 role 부여 방법**: 최초 배포 시 수동으로 Firestore 콘솔에서 `users/{내uid}.role = "child"`를 설정하는 방식으로 충분한지, 아니면 초대 코드 같은 온보딩 플로우가 필요한지.
3. **오프라인 대비**: 기록 화면에서 네트워크가 끊긴 상태로 체크했을 때 동기화 방식 (Firestore 오프라인 캐시 기본 기능으로 충분한지, 별도 안내 문구가 필요한지).
4. **알림 시간 커스터마이징**: v1은 고정 8시로 갈지, 설정 화면에서 부모님/자녀가 시간을 바꿀 수 있게 할지.

---

## 8. 기술 스택 & 배포

| 항목 | 선택 |
|---|---|
| 프론트엔드 | React (기존 프로젝트와 동일 컨벤션) |
| 인증 | Firebase Authentication (Google 로그인 + Anonymous Auth 병행) |
| 데이터베이스 | Firebase Firestore (신규 프로젝트, 기존 자산 대시보드와 분리) |
| 알림 | Firebase Cloud Messaging |
| 배포 | GitHub Pages (신규 저장소, 예: `chair-exercise-tracker`) |

---

## 9. 향후 확장 아이디어 (v1 범위 아님, 참고용)

- 운동 영상/이미지 첨부로 정확한 자세 안내
- 주간/월간 리포트를 자녀에게 카카오톡 알림으로 요약 전송
- 통증 관련 메모 키워드 자동 하이라이트
